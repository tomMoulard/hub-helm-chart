
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: catalogs.hub.traefik.io
spec:
  group: hub.traefik.io
  names:
    kind: Catalog
    listKind: CatalogList
    plural: catalogs
    singular: catalog
  scope: Cluster
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.urls
          name: URLs
          type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Catalog defines a catalog.
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: The desired behavior of this catalog.
              properties:
                customDomains:
                  description: CustomDomains are the custom domains under which the
                    API will be exposed.
                  items:
                    type: string
                  type: array
                services:
                  description: Services are the list of Services available in the Catalog.
                  items:
                    description: CatalogService defines how a Kubernetes Service is
                      exposed within a Catalog.
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                      openApiSpecUrl:
                        type: string
                      pathPrefix:
                        type: string
                      port:
                        type: integer
                    required:
                      - name
                      - namespace
                      - pathPrefix
                      - port
                    type: object
                  type: array
              type: object
            status:
              description: The current status of this catalog.
              properties:
                devPortalDomain:
                  description: DevPortalDomain is the Domain for accessing the dev portal.
                  type: string
                domains:
                  description: Domains are the domains of the Catalog API.
                  items:
                    type: string
                  type: array
                specHash:
                  description: SpecHash is a hash representing the CatalogSpec
                  type: string
                syncedAt:
                  format: date-time
                  type: string
                urls:
                  description: URLs are the URLs for accessing the Catalog API.
                  type: string
                version:
                  type: string
              required:
                - devPortalDomain
                - domains
                - urls
              type: object
          type: object
      served: true
      storage: true
      subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
